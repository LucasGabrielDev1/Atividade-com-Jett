const soma = (a, b) => a + b;
const dobro = valor => valor * 2;
const divisao = (a, b) => a / b;
const porcentagem = (p, valor) => (p / 100) * valor;

module.exports = { soma, dobro, divisao, porcentagem };